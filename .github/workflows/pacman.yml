name: Pac-Man Contributions

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  pacman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Generate Pac-Man contributions
        uses: hirokis/gh-pacman@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          username: L-iuxi
 
